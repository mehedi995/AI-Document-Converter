{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Microsoft.EntityFrameworkCore.Database.Command": "Warning"
    }
  },
  "ConnectionStrings": {
    "__comment": "NO CONNECTION STRING HERE ON PURPOSE (SEC-003). The real one, which contains a password, belongs in user secrets so it never enters source control. Set it with: dotnet user-secrets set \"ConnectionStrings:Default\" \"Host=localhost;Port=5432;Database=adc_dev;Username=adc_app;Password=<your password>\" --project src/AI.Document.Converter.Web"
  }
}
